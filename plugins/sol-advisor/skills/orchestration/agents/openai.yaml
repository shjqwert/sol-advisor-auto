interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Route bounded agents with conditional verification"
  default_prompt: "Use $orchestration to keep complex implementation in this session, delegate only bounded work, and apply conditional independent verification."

policy:
  allow_implicit_invocation: false
