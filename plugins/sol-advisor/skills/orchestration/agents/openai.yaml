interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Focused native agent collaboration"
  default_prompt: "Use $orchestration to delegate by task pattern, select GPT-6 effort at spawn, and coordinate evidence with clear ownership."

policy:
  allow_implicit_invocation: true
