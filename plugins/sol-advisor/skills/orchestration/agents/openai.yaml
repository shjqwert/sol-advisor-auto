interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Quality-gated total-cost delegation"
  default_prompt: "Use $orchestration for engineering work with long sources, unknown search, focused production, frozen implementation plans, or independent adversarial review to decide whether bounded delegation preserves quality and lowers total workflow cost; otherwise use zero children."

policy:
  allow_implicit_invocation: true
