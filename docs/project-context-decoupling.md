# 上下文插件解耦适配

状态：已修改、待测试。测试须在用户确认后执行；本次不发布版本、不打标签或打包。

本次按已确认的独立 project-setup 方案调整当前指导及技能契约。重要决定复用已有设计、ADR 或已授权的 Issue；不建立额外上下文状态系统。保留历史记录和用户修改，不迁移已安装插件。

## 待验收行为

- 无 Project Context、无 `.agent` 的项目仍可按现有授权使用有界委派，不要求先执行 project-setup。
- 已有 implicitDelegation=false 或项目禁用规则仍生效；停用上下文插件不能解除禁用。
- 子代理完成只返回当前任务结果，不自动生成计划、交接或 ADR；主窗口按已有授权维护对应设计记录。
- 用户反悔时修订原决定并保留理由，不把测试通过作为新目标批准。

获准后在隔离项目中观察实际行为，记录输入、模型、工具调用、产物差异与未执行项。先做相关技能/契约检查，只有具体失败证据需要时才扩大验证；静态检查不能代替模型行为验收。若使用 Luna，结果明确标为 Luna 验收，不冒充 GPT-6 实测。
